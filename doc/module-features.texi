@multitable @columnfractions 1
@headitem Feature name @c @tab Description
@item Function count @c @tab Number of functions in the module
@item SCC count @c @tab Number of strongly connected components in the module
@item Integer returning function count @c @tab Number of functions returning integers
@item Floating point returning function count @c @tab Number of functions returning floating-point values
@c
@c loops
@item Min loop depth @c @tab Min loop depth in functions in the module
@item Max loop depth @c @tab Max loop depth in functions in the module
@item Mean loop depth @c @tab Average loop depth in functions in the module
@item Median loop depth @c @tab Median loop depth in functions in the module
@item Loops of depth 1 @c @tab Count of loops of depth 1
@item Loops of depth 2 @c @tab Count of loops of depth 2
@item Loops of depth >2 @c @tab Count of loops of depth >2
@c
@c functions
@item Min arguments to function @c @tab Min number of arguments to functions
@item Max arguments to function @c @tab Max number of arguments to functions
@item Range of arguments to function @c @tab Difference between the max and min number of arguments to functions
@item Mean arguments to function @c @tab Average number of arguments to functions
@item Median argument to function @c @tab Median number of arguments to functions
@c @item Min cylomatic complexity of function @c @tab Min cyclomatic complexity of functions
@c @item Max cylomatic complexity of function @c @tab Max cyclomatic complexity of functions
@c @item Range of cylomatic complexity of functions @c @tab Difference between the max and min cyclomatic complexity of functions
@c @item Mean cylomatic complexity of function @c @tab Average cyclomatic complexity of functions
@c @item Median cylomatic complexity of function @c @tab Median cyclomatic complexity of functions
@item Min CFG edges in functions @c @tab Min number of control flow graph edges of functions
@item Max CFG edges in functions @c @tab Max number of control flow graph edges of functions
@item Range of CFG edges in functions @c @tab Difference between the max and min number of control flow graph edges of functions
@item Mean number of CFG edges in functions @c @tab Average number of control flow graph edges in functions
@item Median number of CFG edges in functions @c @tab Median number of control flow graph edges in functions
@item Min abnormal CFG edges in functions @c @tab Min number of abnormal control flow graph edges of functions
@item Max abnormal CFG edges in functions @c @tab Max number of abnormal control flow graph edges of functions
@item Range of abnormal CFG edges in functions @c @tab Difference between the max and min number of abnormal control flow graph edges of functions
@item Mean number of abnormal CFG edges in functions @c @tab Average number of abnormal control flow graph edges in functions
@item Median number of abnormal CFG edges in functions @c @tab Median number of abnormal control flow graph edges in functions
@c @item Min critical path length of functions @c @tab Min critical path length of functions
@c @item Max critical path length of functions @c @tab Max critical path length of functions
@c @item Range of critical path length of functions @c @tab Different between the max and min critical path length of functions
@c @item Mean critical path length of functions @c @tab Average critical path length of functions
@c @item Median critical path length of functions @c @tab Median critical path length of functions
@c
@item Loop count @c @tab Number of loops in the module
@item Max loop count @c @tab Max number of loops in a function
@item Min loop count @c @tab Min number of loops in a function
@item Mean loop count @c @tab Average number of loops in a function
@item Median loop count @c @tab Median number of loops in a function
@c
@item Basic block count @c @tab Number of basic blocks in the module
@item Max basic block count @c @tab Max number of basic blocks in a function
@item Min basic block count @c @tab Min number of basic blockss in a function
@item Mean basic block count @c @tab Average number of basic blocks in a function
@item Median basic block count @c @tab Median number of basic blocks in a function
@item Basic blocks in loop @c @tab Number of basic blocks in a loop in the module
@item Max basic blocks in loop @c @tab Max number of basic blocks in a loop in a function
@item Min basic blocks in loop @c @tab Min number of basic blocks in a loop in a function
@item Range of basic blocks in loop @c @tab Difference between the max and min number of basic blocks in a loop in a function
@item Mean basic blocks in loop @c @tab Average number of basic blocks in a loop in a function
@item Median basic blocks in loop @c @tab Median number of basic blocks in a loop in a function
@item Basic blocks outside loop @c @tab Number of basic blocks outside a loop in the module
@item Max basic blocks outside loop @c @tab Max number of basic blocks outside a loop in a function
@item Min basic blocks outside loop @c @tab Min number of basic blocks outside a loop in a function
@item Range of basic blocks outside loop @c @tab Difference between the max and min number of basic blocks outside a loop in a function
@item Mean basic blocks outside loop @c @tab Average number of basic blocks outside a loop in a function
@item Median basic blocks outside loop @c @tab Median number of basic blocks outside a loop in a function
@c
@c instructions
@item Instruction count @c @tab Total number of instructions in the module
@item Max instruction count @c @tab Max number of instructions in a function
@item Min instruction count @c @tab Min number of instructions in a function
@item Range of instruction count @c @tab Difference between max and min number of instructions in a function
@item Mean instruction count @c @tab Average number of instructions in a function
@item Median instruction count @c @tab Median instructions in a function
@c
@item Conditional statement count @c @tab Total number of conditional statements in the module
@item Max conditional statement count @c @tab Max number of conditional statements in a function
@item Min conditional statement count @c @tab Min number of conditional statements in a function
@item Range of conditional statement count @c @tab Difference between max and min number of conditional statements in a function
@item Mean conditional statement count @c @tab Average number of conditional statements in a function
@item Median conditional statement count @c @tab Median number of conditional statements in a function
@c
@item Direct call count @c @tab Total number of direct calls in the module
@item Max direct call count @c @tab Max number of direct calls in a function
@item Min direct call count @c @tab Min number of direct calls in a function
@item Range of direct call count @c @tab Difference between max and min number of direct calls in a function
@item Mean direct call count @c @tab Average number of direct calls in a function
@item Median direct call count @c @tab Median number of direct calls in a function
@c
@item Indirect call count @c @tab Total number of indirect calls in the module
@item Max indirect call count @c @tab Max number of indirect calls in a function
@item Min indirect call count @c @tab Min number of indirect calls in a function
@item Range of indirect call count @c @tab Difference between max and min number of indirect calls in a function
@item Mean indirect call count @c @tab Average number of indirect calls in a function
@item Median indirect call count @c @tab Median number of indirect calls in a function
@c
@item Integer operations count @c @tab Total number of integer operations in the module
@item Max integer operations count @c @tab Max number of integer operations in a function
@item Mean integer operations count @c @tab Average number of integer operations in a function
@item Median integer operations count @c @tab Median number of integer operations in a function
@c
@item Floating point operations count @c @tab Total number of floating point operations in the module
@item Max floating point operations count @c @tab Max number of floating point operations in a function
@item Mean floating point operations count @c @tab Average number of floating point operations in a function
@item Median floating point operations count @c @tab Median number of floating point operations in a function
@c
@item Unary operations count @c @tab Total number of unary operations in the module
@item Max unary operations count @c @tab Max number of unary operations in a function
@item Mean unary operations count @c @tab Average number of unary operations in a function
@item Median unary operations count @c @tab Median number of unary operations in a function
@c
@item Pointer arithmetic operations count @c @tab Total number of pointer arithmetic operations in the module
@item Max pointer arithmetic operations count @c @tab Max number of pointer arithmetic operations in a function
@item Mean pointer arithmetic operations count @c @tab Average number of pointer arithmetic operations in a function
@item Median pointer arithmetic operations count @c @tab Median number of pointer arithmetic operations in a function
@c
@item Unconditional branches count @c @tab Total number of unconditional branches in the module
@item Max unconditional branches count @c @tab Max number of unconditional branches in a function
@item Mean unconditional branches count @c @tab Average number of unconditional branches in a function
@item Median unconditional branches count @c @tab Median number of unconditional branches in a function
@c
@item Assign operations count @c @tab Total number of assignment operations in the module
@item Max assign operations count @c @tab Max number of assignment operations in a function
@item Mean assign operations count @c @tab Average number of assignment operations in a function
@item Median assign operations count @c @tab Median number of assignment operations in a function
@c
@item Switch statements count @c @tab Total number of switch statements in the module
@item Max switch statements count @c @tab Max number of switch statements in a function
@item Mean switch statements count @c @tab Average number of switch statements in a function
@item Median switch statements count @c @tab Median number of switch statements in a function
@c
@item Phi nodes count @c @tab Total number of phi nodes in the module
@item Max phi nodes count @c @tab Max number of phi nodes in a function
@item Mean phi nodes count @c @tab Average number of phi nodes in a function
@item Median phi nodes count @c @tab Median number of phi nodes in a function
@item Phi header nodes count @c @tab Total number of phi header nodes in the module
@item Max phi header nodes count @c @tab Max number of phi header nodes in a function
@item Mean phi header nodes count @c @tab Average number of phi header nodes in a function
@item Median phi header nodes count @c @tab Median number of phi header nodes in a function
@end multitable
